import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.phootoo.app',
  appName: 'phootoo',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
